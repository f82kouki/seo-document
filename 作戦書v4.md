# 作戦書 v4：美容マーケ向け「比較記事」SEO半自動システム（Playwright調査 + Claude Code執筆）

## 0. このバージョンの位置づけ（v3からの主要変更＝ハイブリッド化）

**最重要の方針変更：Anthropic APIの従量課金を使わない。** 執筆はAnthropic API（`writer.ts`）ではなく、**Claude Code内で人間が指示して行う**。スクリプトの役割は「調査して research.json と執筆プロンプトファイルを生成するところまで」。これによりAPI課金ゼロを実現する。

役割分担（ハイブリッド）：
- **スクリプト（自動・課金ゼロ）**：discover（比較対象発掘）→ 承認 → serp（競合の切り口調査）→ officialFetch（公式から料金/機能取得）→ research.json生成 → **執筆プロンプトファイル `output/prompts/{slug}.md` を生成**
- **人間 in Claude Code（課金ゼロ）**：生成されたプロンプトファイルのパスをClaude Codeに渡し「これを読んで記事を書いて、中立性・薬機法もチェックして」と指示 → 出力を最終確認 → CMSへ

> v3との差分：`writer.ts`（API呼び出し＋Prompt Caching）を**廃止**。代わりに `promptBuilder.ts`（research.json＋体裁仕様＋中立性ルールを1枚のプロンプトmdに組み立てる）を新設。compliance検査もAPIで自動実行する形をやめ、**Claude Codeでの執筆と同時にセルフチェック**させる方式へ（正規表現版 `check` はオプションで残す）。

---

v3は「インスタマーケツール◯◯選」「UGCツール比較」といった **他社サービスの比較記事** を主戦場とする方針で、これはv4でも不変。比較記事はリスク構造が特殊。

| 観点 | v2（ノウハウ記事） | v3（比較記事） |
|---|---|---|
| 最大リスク | 薬機法（化粧品56効能） | **他社ネガティブ表現** + 景表法・ステマ + 情報鮮度 |
| 扱うデータ | 一般的な手順・知識 | **他社の固有名・料金・機能（事実性が命）** |
| 鮮度 | 比較的安定 | 料金・プランは頻繁に変わる（陳腐化が速い） |
| 法的射程 | 薬機法 | 景表法（比較広告）・ステマ規制・商標/名誉毀損 |

絶対条件（ユーザー指定）：
- **他社を落とす文言は一切入れない。** 「A社は機能が貧弱」「B社は割高」のような相対的ネガティブ表現を一切排除する。
- **AI以外に金銭コストをかけない。** 外部API（SerpAPI / Bing Web Search / 有料DB）は不使用。Playwright直叩きと公開JSON endpointのみ。
- **Anthropic APIの従量課金も使わない。** 執筆はClaude Code内で行い、スクリプトはAPIを一切呼ばない（v4の中核制約）。

確定済みの方針（ユーザー提供の参考リンクより）：
- 一次情報は **公式ドメインを正** とする（例：Instagramヘルプ、消費者庁）。記事の信頼性も参考リンクもここに依存させる。
- ステマ規制の主体は **広告主である自社** （消費者庁の運用基準より、依頼を受けた第三者ではなく事業者が規制対象）。よって比較記事を出す自社こそがステマ表示義務の当事者。

---

## 1. 比較記事の「3大リスク」と設計上の対応（v3の核心）

### リスク1：他社ネガティブ表現（最優先・絶対NG）
比較記事で最も事故が起きやすい。正規表現だけでは「A社は〇〇が弱い」のような相対表現を拾いきれないため、**多層防御**にする。

- **層1：執筆方針でそもそも生ませない（予防）**
  system promptに「全社を等しく肯定的に紹介する。短所・欠点・否定・優劣の断定を書かない。各社は『向いている人/ユースケース』の差として記述する」を強制。比較は優劣ではなく **適合（fit）の違い** として表現させる。
- **層2：正規表現での明示NG検出（compliance.ts 比較層）**
  「貧弱／劣る／物足りない／割高／高すぎ／使いにくい／おすすめできない／〜には不向き／デメリットしかない」等のネガティブ語を、**他社名の近傍で**検出して警告。
- **層3：Claude Code執筆時のセルフチェック**
  執筆指示に「他社を貶める/優劣を断定する表現が無いか自己点検し、あれば直す」を含める。ただし**書いた本人による点検なので見落としは出る前提**（補助線であって保証ではない）。正規表現版の `check` コマンドも任意で併用可。
- **層4：人間レビュー（必須・最終責任）**
  自動検出もセルフチェックも補助線。最終的な「落としていないか」の判断は人間。ここは省略不可。

> 設計思想：比較記事は「どれが一番良いか」ではなく「あなたにはどれが合うか」を提示するもの、と定義を固定する。これがネガティブ表現を構造的に減らす。

### リスク2：景表法・ステマ規制
- 自社サービスを比較対象に含める場合、**自社が広告主＝ステマ規制の当事者**。記事が自社の宣伝を兼ねるなら、その旨が分かる表示（運営者明記・PR表記の検討）が必要。
- アフィリエイトリンク（他社サービスへの紹介リンクで報酬が発生する形）を貼る場合も広告性が生じるため、ステマ層で検査。
- 「No.1」「最も人気」「業界唯一」等の **優良誤認** につながる最上級表現を景表法層で検出（根拠となる調査出典がJSONに無ければ警告）。

### リスク3：情報鮮度（比較記事特有）
料金・プラン・機能は陳腐化が速い。誤った料金掲載は他社への不利益にも、自社の信頼失墜にもなる。
- research.jsonに各データの **取得日（fetchedAt）と出典URL** を必ず保持。
- 記事末尾に「※料金・機能は YYYY年MM月 時点。最新は各公式サイトでご確認ください」の **鮮度ディスクレーマーを自動挿入**。
- 料金などの数値は **公式サイト由来のみ** をJSONに入れ、二次情報（他社のまとめ記事）の数値は採用しない（伝言ゲームでの誤記防止）。

---

## 2. データ取得方針（金銭コストゼロ）

ユーザーからの選択肢回答は明示されなかったが、提供された参考リンク（すべて公式ドメイン）が方針を物語っている。**「公式サイト直叩きを一次情報の正とし、SERPは調査の起点としてのみ使う」ハイブリッド**を採用する。これが鮮度・正確性・他社への公平性すべてに最も資する。

### 2-1. 比較対象リスト（他社サービス）の決め方 ★AI主導・人間は承認だけ
比較対象の台帳づくりも **AIに探させる**。人間の作業は「AIが出した候補にOKを出す」ワンステップだけに圧縮する。
1. **AIが候補を自動発掘**：`npm run discover -- --keyword="インスタ 分析ツール"` を実行。`discover.ts` がSERP上位の比較記事を開いて登場するサービス名を抽出し、各サービスの公式ドメイン・料金ページURLまで自動で推定・収集して **候補リスト（`output/candidates/{keyword}.csv`）** を出力する。
2. **人間は承認だけ**：候補CSVに `approve` 列（既定 `?`）が付く。人間は採用するものを `y`、外すものを `n` にするだけ（理由は任意でnoteに）。URLを手で探したり貼ったりはしない。
3. **承認分を台帳に反映**：`npm run discover -- --commit` で `approve=y` の行を `tools.csv` に追記。公式URL/料金URLは discover が取得済みなので人間は触らない。取得に失敗したサービスは候補段階で warning 表示し、人間が「採用するなら手当て」を判断（推測URLは入れない）。

> なぜ「承認だけ」は残すのか：比較記事は「どの会社を載せ／外すか」が編集判断そのもの。完全自動で除外まで任せると「特定他社を不当に外した」と取られ得る。だから**発掘・URL収集はAI、最終的な採否のハンコだけ人間**という分担にする。承認はCSVのy/nを打つだけなので実質ワンクリック。

### 2-2. SERPエンジン（調査の起点のみ・直叩き）
v2の方針を踏襲しつつ、用途を「競合がどんな見出し・切り口で書いているか」の把握に限定。**料金・機能の数値はSERPからは採らない**（公式から採る）。
- 優先順：**DuckDuckGo HTML（`html.duckduckgo.com/html/`）→ Bing HTML → Google**（前段が0件/CAPTCHAで次へ）
- 対策：実在ブラウザ相当UA・`locale: 'ja-JP'`・viewport設定、リクエスト間 1.5〜3秒ランダムthrottle、並列度2、CAPTCHA/consent検知で次エンジン
- **重要：3エンジン全滅でもパイプラインは止めない**。SERPはあくまで起点。取得失敗時は「公式サイト直叩き（tools.csvのURL）」だけで research を成立させる degrade パスを必須実装（v2で未定義だった穴を塞ぐ）。

### 2-3. 公式サイト直叩き（一次情報・最重要）
- tools.csv の各公式URL（できれば料金・機能ページ）をPlaywrightで開く
- 抽出：サービス名、料金プラン名と価格、主要機能、対象ユーザー、最終更新日（`<time>`/meta `article:modified_time`）
- throttle 1〜2秒・並列度2・robots常識の範囲で軽量取得
- 取得した数値・文言は **そのままJSONに保存し、出典URLと取得日を必ず付与**

### 2-4. サジェスト拡張（任意・公開JSON）
`suggestqueries.google.com/complete/search`（CAPTCHA無し・軽量・合法）で関連キーワードを取得し、記事内の見出し候補やFAQ候補に使う。

> 法的・運用上の注記：SERPのHTML直叩きは各検索エンジンの規約に抵触し得る点、IP banで突然停止し得る点はv3でも変わらないリスク。だからこそv3では **SERPを「あれば便利な起点」に格下げし、記事の根拠は公式直叩きに移した**。SERPが死んでも記事は作れる構造にしてある。

---

## 3. ディレクトリ構成

```
beauty-seo-articles/
├── src/
│   ├── pipeline.ts        # メイン。topics選定→research→promptBuilder→保存→CSV更新（APIは呼ばない）
│   ├── research.ts        # Playwright調査（SERP起点 + 公式直叩き一次情報 + 共起語）
│   ├── serp.ts            # SERPエンジン抽象化（DuckDuckGo→Bing→Google／全滅でもdegrade）
│   ├── officialFetch.ts   # tools.csvの公式URLから料金/機能/更新日を抽出
│   ├── discover.ts        # 比較対象サービス候補を自動発掘（人間は承認のみ）
│   ├── promptBuilder.ts   # ★v4新規：research.json＋体裁仕様＋中立性ルール→執筆プロンプトmd生成
│   ├── check.ts           # ★任意：生成済み記事mdを正規表現でcompliance検査（npm run check）
│   ├── neutralityCheck.ts # 他社ネガティブ表現の正規表現チェック（check.tsから利用）
│   ├── template.ts        # 比較記事の体裁仕様（promptBuilderに供給）
│   ├── urlValidator.ts    # 参考リンクがresearch.json由来か照合 + 死活チェック
│   ├── freshness.ts       # 鮮度ディスクレーマー文面生成 + 料金データの取得日検証
│   ├── config.ts          # 並列度・throttle 設定（APIキーは不要）
│   └── types.ts           # Topic / ToolEntry / ResearchResult / Warning 等
├── data/
│   ├── topics.csv         # 記事テーマ（slug,title,keyword,category,status,priority,note）
│   ├── tools.csv          # 比較対象サービス（name,official_url,pricing_url,category,note）
│   ├── ng-words.json      # 比較ネガティブ表現 + 景表法最上級表現 + 薬機法NG + 56効能ホワイトリスト
│   └── sources.json       # 信頼できる一次情報ドメイン（公式・規制当局）
├── output/
│   ├── research/{slug}.json   # 調査結果（料金/機能＋出典URL＋取得日）
│   ├── prompts/{slug}.md      # ★v4新規：Claude Codeに読ませる執筆プロンプト
│   ├── candidates/{kw}.csv    # discoverの承認待ち候補
│   └── drafts/{slug}.md       # ★Claude Codeで執筆した記事を人間がここへ保存
├── prompts/
│   ├── system.md          # 比較記事テンプレ仕様 + 中立性ルール + 規制ガイドライン（promptBuilderが読む本体）
│   └── examples/          # few-shot用（差し込み式）
│       ├── pr-hyoki-guide.md  # 支給済みサンプル（体裁の正）
│       └── .gitkeep       # 追加サンプルはここに置けば自動で反映
├── docs/
│   └── 作戦書.md
├── .gitignore             # node_modules, output/ を除外（.envは不要）
├── tsconfig.json
└── package.json
```

> v4ではAPIキーが不要なので `.env` / `.env.example` は廃止。スクリプトはClaudeを一切呼ばない（Playwrightと公開JSONのみ）。

---

## 4. モジュール仕様

### `types.ts`
```ts
type Topic = { slug, title, keyword, category, status, priority, note }
type ToolEntry = { name, officialUrl, pricingUrl?, category, note? }  // ★比較対象
type SerpEntry = { rank, url, title, snippet, h2: string[], h3: string[], faq: string[], lastUpdated?: string }
type OfficialData = {            // ★公式直叩きの一次情報
  toolName, sourceUrl, fetchedAt,
  plans: { name, price, billingCycle? }[],
  features: string[],
  targetUser?: string,
  lastUpdated?: string
}
type ResearchResult = {
  slug, keyword, fetchedAt, engine,
  serp: SerpEntry[],             // 起点（切り口把握用）
  officialData: OfficialData[],  // ★記事の数値根拠はここだけ
  coOccurringTerms: string[]
}
type Warning = {
  layer: '比較中立性'|'景表法'|'ステマ'|'薬機法'|'鮮度'|'URL',
  severity: 'high'|'mid',
  match: string, context: string, line?: number
}
type Article = { slug, markdown, warnings: Warning[], referencedUrls: string[] }
```

### `serp.ts`（起点取得・直叩き・degrade必須）
- `fetchSerp(keyword, page): Promise<{engine, results}>`、DuckDuckGo→Bing→Googleフォールバック
- DuckDuckGo結果 `.result__a`、Bing `#b_results .b_algo h2 a`
- **全滅時は空配列を返し、例外を投げない**（research.tsが公式直叩きだけで続行できるように）
- `playwright` npmパッケージをコードから直接駆動（MCPではない）

### `officialFetch.ts`（★一次情報の正・新規）
**役割：他社の料金・機能を「公式から正確に」採る。比較記事の事実性の生命線。**
1. tools.csv の各 `officialUrl` / `pricingUrl` をPlaywrightで開く
2. 料金プラン・機能・対象ユーザー・最終更新日を抽出
3. **数値は公式由来のみ採用。二次情報の数値は捨てる**（誤記の伝播防止）
4. 各データに `sourceUrl` と `fetchedAt` を必ず付与 → freshness/urlValidator が後で使う
5. 取得失敗（ページ改変等）はそのサービスを warning にし、人間に「公式を手動確認」を促す（推測で埋めない）

### `discover.ts`（★比較対象の自動発掘・新規・主役）
**役割：人間に代わって「比較すべき他社サービス」を探し、公式/料金URLまで揃えて承認待ち候補にする。**
1. `npm run discover -- --keyword="..."`：SERP上位の比較記事を複数開き、登場するサービス名を抽出・名寄せ（表記ゆれ統合）
2. 各サービスの **公式ドメインと料金ページURLを自動推定・検証**（公式サイトをPlaywrightで開いてタイトル/会社名が一致するか確認。推測のままにしない）
3. 出力：`output/candidates/{keyword}.csv`（列：name, official_url, pricing_url, 出現回数, approve[既定`?`], note）。出現頻度や情報取得可否でソート
4. URL取得に失敗したサービスは候補に残しつつ warning 明示（人間が採用するなら手当てを判断）
5. `npm run discover -- --commit`：`approve=y` の行だけを `tools.csv` に追記（重複は名寄せでスキップ）
- **自動確定はしない**＝採否のハンコは必ず人間。これが恣意的除外リスクへの担保

### `promptBuilder.ts`（★v4の核・writer.tsを置き換え）
**役割：Claude Codeに読ませる「執筆プロンプト1枚」を組み立てて `output/prompts/{slug}.md` に保存する。APIは呼ばない。**
出力するプロンプトmdの中身は、Claude Codeがそれだけ読めば記事を書けるよう、以下を1ファイルに連結する：
1. **執筆指示**：「あなたは美容マーケのSEO編集者。以下の体裁と中立性ルールに厳密に従い、research部の数値だけを使って比較記事を書く。書き終えたら中立性・薬機法のセルフチェックも行う」
2. **体裁仕様**：`prompts/system.md` ＋ `template.ts` の固定構造（後述の体裁）
3. **few-shot**：`prompts/examples/*.md`（pr-hyoki-guide.md 等）を参考例として添付
4. **中立性ルール**：全社を等しく肯定的に・優劣の断定禁止・違いはfit軸で・料金機能はresearch由来のみ・新規URL生成禁止
5. **research部**：`research.json` の officialData（料金/機能/出典URL/取得日）と competitor見出し・共起語を、読みやすい形で埋め込む
6. **セルフチェック指示**：「①他社を貶める/優劣断定が無いか ②薬機法の断定表現（56効能外）が無いか ③価格はresearch部の数値と一致するか ④参考リンクはresearch部のURLのみか を自己点検し、該当あれば修正して、末尾に『セルフチェック結果』を箇条書きで付す」
7. **鮮度ディスクレーマー文面**：`freshness.ts` が生成した「※◯年◯月時点」の一文を埋め込み、記事末尾に必ず入れさせる

**運用**：人間はClaude Codeで「`output/prompts/insta-analytics.md` を読んで記事を書いて」と指示するだけ。出力記事は人間が `output/drafts/{slug}.md` に保存。

> セルフチェックの限界を明記：書いた本人が点検するため、暗示的な他社ネガティブは見落とし得る。**最終責任は人間レビュー**。不安なら `npm run check` の正規表現検査を重ねる。

### `check.ts`（★任意・生成済み記事の正規表現検査）
`npm run check -- --file=output/drafts/{slug}.md` で、Claude Codeが書いた記事を**機械的に再点検**する。セルフチェックの見落としを拾う二重確認用（使うかは任意）。結果は記事末尾の `<!-- COMPLIANCE WARNINGS -->` ブロックに追記し、先頭に固定免責文「この自動チェックは明示的NG語のみ。暗示表現は対象外。最終判断は人間」を置く。検査層：
- **比較中立性層（最優先）**：`neutralityCheck.ts` を呼ぶ。ng-words.jsonの「比較ネガティブ」語（貧弱/劣る/物足りない/割高/使いにくい/おすすめできない/不向き 等）を tools.csv の**他社名の近傍**で検出 → high
- **景表法層**：「No.1／最も／業界唯一／最高／最安」等の最上級を検出。根拠出典がresearch.jsonに無ければ優良誤認リスクとして warning
- **ステマ層**：自社含む or アフィリ時、運営者・PR表記の有無を検査（topics.csv noteの `案件/提供/自社PR` フラグと突合）
- **薬機法層**：56効能ホワイトリスト＋NG正規表現
- **鮮度層**：`freshness.ts` を呼び、料金がresearch.json由来か・「◯年◯月時点」記載があるかを検査
- **URL層**：`urlValidator.ts` を呼び、参考リンクがresearch.json由来か正規化照合

### `neutralityCheck.ts` / `freshness.ts` / `urlValidator.ts`（check.tsの部品）
- **neutralityCheck**：他社名近傍のネガティブ語を正規表現検出。検出＝自動修正はせず人間判断材料を出すだけ
- **freshness**：鮮度ディスクレーマー文面（「※料金・機能は◯年◯月時点。最新は各公式サイトで要確認」）の生成と、research.jsonのfetchedAtが古すぎ（例30日超）の警告、research.jsonに無い価格が本文にあれば捏造疑いhigh
- **urlValidator**：正規化（プロトコル/末尾スラッシュ/クエリを揃える）後の完全一致で照合＋任意で死活200チェック

### `template.ts`（★記事体裁を確定仕様化）
ユーザー提供サンプル（PR表記実務ガイド）の体裁を **正式テンプレート**として固定する。`template.ts` はこの骨格と記法をsystem promptに供給し、サンプル未支給時もこの構造が品質の最低保証になる。

**記事の固定構造（この順序・この記法）：**
```
{記事タイトル}

狙いキーワード：{keyword}（必要なら複数 / で併記）
カテゴリ：{category}

{リード文：読者の具体的な悩みを「」で提起 → 本記事が一次情報をもとに整理する旨を宣言。200-300字}

※{冒頭ディスクレーマー：一般的な情報提供である旨／最終判断は公式・専門家確認に基づく旨}


■ {見出し1：定義・背景。一次情報の出典を明記}
{本文}
▸ {小見出し：▸で示す}
・{箇条書き：・で示す}

■ {見出し2：誰が・何を・どこに 等の実務分解}

■ {見出し3：対象別・SNS別などの並列セクション。各社/各項目を中立に列挙}
▸ 重要：{注意喚起の小見出し}

■ {美容で特に注意：薬機法との接続を必ず一節置く（56効能・断定表現NG）}

■ よくある質問（FAQ）
Q. {質問}
A. {回答}

■ まとめ
{要点を①②③の形で凝縮}

■ 参考リンク（公式・一次情報を優先）
・{媒体名「ページ名」：URL}  ← research.json由来のみ

────────────────────────
※公開前チェック
・{施行日・条項・仕様は一次情報で再確認すべき旨}
・{薬機法など同時に留意すべき点}
```

**体裁ルール（promptBuilderが執筆プロンプトに反映）：**
- 見出しは `■`、小見出しは `▸`、箇条書きは `・`（Markdownの#や-ではなくこの記法で統一）
- リード冒頭は読者の悩みを「」で具体提起する型
- **本文中に「一次情報をもとに」「公式より」と出典依拠を明示する**トーン（E-E-A-T）
- 「美容で特に注意」節と末尾「※公開前チェック」は **必須ブロック**（省略不可）
- FAQは `Q.` / `A.` 形式
- 参考リンクは「公式・一次情報を優先」と銘打ち、research.json由来URLのみ
- 比較記事の場合：上記「対象別セクション」を各社紹介に充て、中立・fit軸（2-1の中立性ルール）で記述

> このサンプルは `prompts/examples/pr-hyoki-guide.md` として同梱。few-shotの実物第1号とし、以降ユーザーがexamples/にファイルを足せば自動でfew-shotに加わる（差し込み式）。

### `urlValidator.ts` / `config.ts`
v2同様。urlValidatorは正規化後の完全一致で照合＋任意で死活200チェック。

### `pipeline.ts`
`npm run prep -- --count=5 [--only=slug]`（旧 generate。APIを呼ばないので「執筆準備＝prep」に改名）
1. `topics.csv` から `status=todo` を priority降順でN件
2. 各テーマ：research（serp起点＋official直叩き）→ **promptBuilder → `output/prompts/{slug}.md` と `output/research/{slug}.json` を保存**（執筆はしない）
3. プロンプト保存成功を確認してからCSVを `status=ready` 更新（失敗時 `status=error`。原子性確保）
4. 並列度：Playwright 2並列（API並列は無し）

> ここまでが自動。この後、人間がClaude Codeで各 `output/prompts/{slug}.md` を開いて執筆 → `output/drafts/{slug}.md` に保存 → 必要なら `npm run check` → 公開判断時に手動で `status=done`。

---

## 5. CSVスキーマ

### topics.csv（記事テーマ）
```csv
slug,title,keyword,category,status,priority,note
insta-analytics-tools,インスタ分析ツール比較7選,Instagram 分析ツール 比較,比較,todo,1,自社サービス含む=ステマ表記必須
ugc-tools-compare,UGCツール比較ガイド,UGC ツール 比較,比較,todo,1,
```

### tools.csv（★比較対象サービス）
```csv
name,official_url,pricing_url,category,note
ServiceA,https://service-a.example.com,https://service-a.example.com/pricing,分析,
ServiceB,https://service-b.example.com,https://service-b.example.com/price,分析,
自社サービス,https://our-service.example.com,https://our-service.example.com/plan,分析,自社=PR表記対象
```

---

## 6. 運用フロー
人間がやるのは **(A)テーマを足す (B)比較対象候補にOKを出す (C)Claude Codeで執筆指示 (D)公開前チェック**。調査（SERP・公式データ収集・プロンプト生成）はスクリプト、執筆はClaude Code（API課金なし）。

| タイミング | アクション | 主体 |
|---|---|---|
| 比較対象の発掘 | `npm run discover -- --keyword="..."` で候補CSV自動生成（公式/料金URL込み） | **スクリプト** |
| 候補の承認(B) | 候補CSVの approve を y/n → `--commit` で tools.csv 反映 | 人間（ハンコのみ） |
| テーマ追加(A) | topics.csv に5〜10本印 | 人間 |
| 調査・準備 | `npm run prep -- --count=5`：research→research.json＋執筆プロンプトmd生成 | **スクリプト** |
| 執筆(C) | Claude Codeで「`output/prompts/{slug}.md` を読んで記事を書いて、中立性・薬機法もチェックして」と指示 | 人間＋Claude Code（課金なし） |
| 記事保存 | 出力を `output/drafts/{slug}.md` に保存。任意で `npm run check` で再点検 | 人間 |
| 公開前チェック(D) | 他社を落としていないか・料金が公式と一致か・署名/体験加筆・`【済み】`付与 | 人間 |
| 入稿 | 手動でCMSへ（スコープ外） | 人間 |

> 調査は無人。執筆はClaude Code内で人間が1指示すれば書ける（コピペ不要、プロンプトファイルのパスを渡すだけ）。API従量課金はどの工程でも発生しない。

---

## 7. コスト（完全ゼロ）
- スクリプトはAnthropic APIを呼ばない（Playwright直叩き＋公開JSONのみ）→ **API従量課金ゼロ**
- 執筆はClaude Code内で実施 → こちらもAPI従量課金は発生しない（利用枠の範囲内）
- 外部検索API・有料DBも不使用 → **追加の金銭コストは原則ゼロ**
- 唯一のコストは人間の時間（執筆指示＋公開前チェック）。比較記事は中立性・料金一致の確認に1本あたり相応の時間を見込む

> 注：執筆をClaude Codeで行うため、APIキー（`ANTHROPIC_API_KEY`）もConsoleでの課金設定も不要。

---

## 8. リスクと対策
| リスク | 対策 |
|---|---|
| 他社を落とす表現（最優先） | 執筆プロンプトで予防＋Claude Code執筆時セルフチェック＋任意のnpm run check（他社名近傍の正規表現）＋人間最終確認 |
| 料金・機能の誤記（他社不利益） | 公式直叩きのみ採用・二次情報の数値は捨てる・出典URL/取得日を必須付与 |
| 情報の陳腐化 | fetchedAt検証＋鮮度ディスクレーマー自動挿入＋古いデータをwarning |
| 景表法（優良誤認） | No.1等の最上級を検出、根拠出典が無ければwarning |
| ステマ規制 | 自社含む/アフィリ時に運営者・PR表記を検査（規制主体は広告主＝自社） |
| SERP直叩きCAPTCHA/IP ban | DuckDuckGo主＋フォールバック、throttle/並列度2。**全滅でも公式直叩きで記事成立するdegrade必須** |
| SERP規約抵触 | SERPを「起点」に格下げし根拠は公式へ。停止時の代替を確保 |
| AIによるURL/数値捏造 | 執筆プロンプトで「research由来のみ」明示＋セルフチェック＋urlValidator（正規化照合）＋freshnessで未出典価格をhigh警告 |
| 自動チェックの過信 | warningブロック先頭に免責文固定。中立性は人間が最終判断 |
| サンプル未支給で品質ブレ | template.tsの比較記事構造をフォールバック保証。examples/に置けば自動反映 |

---

## 9. 実装順（Next Steps）
1. `beauty-seo-articles/` 作成、`npm init` + `typescript`/`tsx`/`playwright`/`csv-parse` 導入（**@anthropic-ai/sdk は不要**）、`npx playwright install chromium`
2. `types.ts` → `config.ts` → `template.ts`（比較記事の体裁仕様）
3. `prompts/system.md`（**最重要**）：比較記事テンプレ＋**中立性ルール**＋規制ガイドを書き起こす。examples/ には pr-hyoki-guide.md を配置済み
4. `ng-words.json`：比較ネガティブ＋景表法最上級＋薬機法NG＋56効能ホワイトリスト
5. `neutralityCheck.ts` + `freshness.ts` + `urlValidator.ts` + `check.ts`（比較記事のリスク検査を先に固める）
6. `serp.ts`（degrade付き）→ `officialFetch.ts` → `discover.ts` → `research.ts`
7. `promptBuilder.ts`（research.json＋体裁＋中立性ルール→執筆プロンプトmd生成）
8. `pipeline.ts` 統合（prep：research→promptBuilder、ステータス更新の原子性）、`docs/作戦書.md` 清書
9. 検証（下記）→ Claude Codeでの執筆運用を開始→ 週5本→安定後10本/週

> モデルIDやPrompt Caching仕様の調査は**不要になった**（APIを使わないため）。実装初日に詰まる地雷が1つ消えた。

---

## 10. 検証方法（end-to-end）
1. **中立性検出テスト**：「ServiceAは機能が貧弱で割高」等を含むダミー記事を `npm run check`（neutralityCheck）に通し、他社名近傍のネガティブが high 警告化されるか
2. **公式データ取得テスト**：tools.csvの公式URLから料金/機能/更新日が取れ、各データに sourceUrl と fetchedAt が付くか
3. **SERP degrade テスト**：serp.tsを意図的に全滅させ、公式直叩きだけで research.json が成立し prep が完走するか
4. **プロンプト生成テスト**：`output/prompts/{slug}.md` が体裁仕様＋research＋中立性ルール＋セルフチェック指示を含む完結した1枚になっているか（Claude Codeがそれだけ読んで書けるか）
5. **執筆テスト**：実際にClaude Codeでプロンプトmdを読ませて1本書かせ、体裁・中立性・参考リンクがresearch由来か目視
6. **数値捏造ガードテスト**：research.jsonに無い価格を含む下書きを `check`（freshness）に通し high 警告化されるか
7. **URL捏造ガードテスト**：research.json に無いURLを含む下書きを urlValidator で警告化
8. **再現テスト**：pr-hyoki-guide.md と構成・トーン・参考リンクを並べて目視比較

---

## 11. 未確定 / 後続で必要な入力
- **APIキー：不要**（執筆はClaude Code内で行い、スクリプトはAPIを呼ばない）
- **比較対象サービス**：`discover.ts` が自動発掘・URL収集する。人間は候補CSVの approve を y/n にするだけ（手動でのURL記入は不要）。初回キーワードだけ人間が指定
- **自社サービスの扱い**：比較に自社を含めるか。含めるならステマ/PR表記の方針を確定
- **サンプル**：1本目（PR表記実務ガイド）支給済み → `prompts/examples/pr-hyoki-guide.md` に同梱、体裁を `template.ts` の確定仕様化。追加サンプルは examples/ に置けば自動でfew-shotに反映（差し込み式）